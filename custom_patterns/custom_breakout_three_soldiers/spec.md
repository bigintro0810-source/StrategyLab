# サンプル: 3連続陽線 + 直近高値ブレイク

動作確認用に手で作ったカスタムパターン。パターン作成画面から作ると、
ここにLLMが書いた日本語仕様書が入る。

## 成立条件

1. 直前3本(現在バーを含む)がすべて陽線(終値 > 始値)
2. 現在バーの終値が、直近 `lookback` 本の高値を上抜けている

## パラメータ

| 名前 | 初期値 | 意味 |
|---|---|---|
| lookback | 20 | 高値を見る本数 |

## 先読み

現在バーより後のデータは一切参照しない。
